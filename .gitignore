# Drop documents here to process — never committed
_inbox/

# API keys — never commit to the repository
js/config.js

# Tunnel binary — local dev tool, not part of the app
cloudflared

# Claude Code internal files — not part of the project
.claude/
PROJECT_MEMORY.md

# System files
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.sublime-workspace
